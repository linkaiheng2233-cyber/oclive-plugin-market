/** 与 creator-docs/roadmap/PLUGIN_WEB_SECTION.md 示例一致 */
export interface PluginListing {
  id: string
  name: string
  description: string
  repo_url: string
  min_oclive_version: string
  tags: string[]
  author: string
}
